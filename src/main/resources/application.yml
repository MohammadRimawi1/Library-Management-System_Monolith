server:
  port: ${PORT}

Spring:
  mongodb:
    uri: ${MONGO_URI}

library:
  admin-email: ${ADMIN_EMAIL}
  admin-password: ${ADMIN_PASSWORD}

  jwt:
    secret: ${JWT_SECRET}
    expiration-ms: 86400000 # 24 hours